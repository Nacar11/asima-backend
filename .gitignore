# Compiled output
dist/
build/
*.tsbuildinfo

# Dependencies
node_modules/

# Environment files
.env
.env.local
.env.*.local
# Deployment credentials for the live demo (Supabase). Named .prod rather
# than .local so nobody reads it as the local-dev config — it points at real
# data, and `db:fresh` with it loaded would drop the production schema.
.env.*.prod

# Logs
logs/
*.log
npm-debug.log*

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Tests / coverage
coverage/
.nyc_output/

# Misc
*.pid
*.seed
*.tgz

# Personal / ever-changing docs — kept local only
tasks/
