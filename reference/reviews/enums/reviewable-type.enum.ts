/**
 * Reviewable Type Enum.
 *
 * Defines the types of entities that can be reviewed.
 *
 * @version 1
 * @since 1.0.0
 */
export enum ReviewableTypeEnum {
  PRODUCT = 'product',
  SERVICE = 'service',
  SELLER = 'seller',
}
