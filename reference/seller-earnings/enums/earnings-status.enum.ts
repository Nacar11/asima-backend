/**
 * Earnings status enumeration.
 *
 * Represents the status of seller earnings from pending to paid.
 *
 * @version 1
 * @since 1.0.0
 */
export enum EarningsStatusEnum {
  PENDING = 'pending',
  AVAILABLE = 'available',
  PROCESSING = 'processing',
  PAID = 'paid',
  HELD = 'held',
}
