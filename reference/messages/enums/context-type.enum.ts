/**
 * Conversation context type enumeration.
 *
 * Defines what the conversation is related to (optional context).
 *
 * @version 1
 * @since 1.0.0
 */
export enum ContextTypeEnum {
  GENERAL = 'general',
  BOOKING = 'booking',
  SALES_ORDER = 'sales_order',
  INQUIRY = 'inquiry',
}
