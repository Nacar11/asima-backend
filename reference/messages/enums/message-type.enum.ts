/**
 * Message type enumeration.
 *
 * Defines the type of message content.
 *
 * @version 1
 * @since 1.0.0
 */
export enum MessageTypeEnum {
  TEXT = 'text',
  IMAGE = 'image',
  FILE = 'file',
  SYSTEM = 'system',
}
