/**
 * Conversation status enumeration.
 *
 * Defines the current status of a conversation.
 *
 * @version 1
 * @since 1.0.0
 */
export enum ConversationStatusEnum {
  ACTIVE = 'active',
  ARCHIVED = 'archived',
  BLOCKED = 'blocked',
}
