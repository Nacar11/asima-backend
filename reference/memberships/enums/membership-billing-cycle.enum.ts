/**
 * @deprecated Use MembershipBillingPeriodEntity instead.
 * Kept for backward compatibility with existing imports during transition.
 */
export enum MembershipBillingCycleEnum {
  MONTHLY = 'MONTHLY',
  ANNUAL = 'ANNUAL',
}
