/** Bcrypt cost factor used by UsersService for password hashing. */
export const BCRYPT_ROUNDS = 10;

/**
 * Email domain used by the seeded directory. Service-layer normalization
 * lowercases all incoming emails — this constant is reference-only.
 */
export const ASIMA_EMAIL_DOMAIN = 'asima.inc';
